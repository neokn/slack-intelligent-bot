{
    "mcpServers": {
        "playwright": {
            "command": "npx",
            "args": [
                "@playwright/mcp",
                "--no-sandbox",
                "--browser", 
                "chrome"
            ]
        }
    }
}
